export enum StatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum DecorStatusEnum {
  NEW = "new",
  ACCEPTED = "accepted",
  DELIVERED = "delivered",
  DONE = "done",
  CANCELED = "canceled",
}
